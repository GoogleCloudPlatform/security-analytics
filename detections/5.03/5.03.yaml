---
id: '5.03'
name: data_amount_accessed_by_user_per_day
# Refer to corresponding queries: 5_03_data_amount_accessed_by_user_per_day.{sql|yaral|...}
display_name: How much data was accessed by each user per day in the past week?
category: Data Usage
severity: Low
description: |
  Amount of data accessed by each user per day in the past week, based on total bytes billed in user's queries.

sources:
  - Audit Logs - Data Access

use_cases:
  - Audit

test:

samples:

references: