---
id: '5.07'
name: BQ_queries_with_large_scans
# Refer to corresponding queries: 5_07_BQ_queries_with_large_scans.{sql|yaral|...}
display_name: Any queries doing very large scans?
category: Data Usage
severity: Low
description: |
  Detect any query that is doing a very large scan (> 1 GB).

sources:
  - Audit Logs - Data Access

use_cases:
  - Audit
  - Detect

test:

samples:

references: