---
id: '6.21'
name: log4j_exploit_attempts
# Refer to corresponding queries: 6_21_log4j_exploit_attempts.{sql|yaral|...}
display_name: Log4j 2 vulnerability exploit attempts
category: Network Activity
severity: Medium
attack_mapping:
  - technique: T1190
    title: Exploit Public-Facing Application
    link: https://attack.mitre.org/techniques/T1190/
description: |
  Detect "Log4j 2" vulnerability exploit attempt against any of your internet-facing applications
  sitting behind HTTP(S) Load Balancer.

sources:
  - HTTP(S) LB Logs

use_cases:
  - Detect

test:

samples:

references: