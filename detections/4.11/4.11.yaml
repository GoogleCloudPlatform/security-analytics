---
id: '4.11'
name: autoscaling_usage_frequency_by_day
# Refer to corresponding queries: 4_11_autoscaling_usage_frequency_by_day.{sql|yaral|...}
display_name: Autoscaling usage per day in the past month
category: Cloud Workload Usage
severity: Low
description: |
  Autoscaling usage per day in the past month

sources:
  - Audit Logs - Admin Activity

use_cases:
  - Audit

test:

samples:

references: