---
id: '5.20'
name: top_data_access_actions
# Refer to corresponding queries: 5_20_top_data_access_actions.{sql|yaral|...}
display_name: Most common data (and metadata) access actions in the past month
category: Data Usage
severity: Low
description: |
  Most common data (and metadata) access actions from any cloud resource in the past month.

sources:
  - Audit Logs - Data Access

use_cases:
  - Audit
  - Detect

test:

samples:

references: