---
id: '5.06'
name: BQ_queries_top
# Refer to corresponding queries: 5_06_BQ_queries_top.{sql|yaral|...}
display_name: Top 10 queries against BigQuery in the past week
category: Data Usage
severity: Low
description: |
  Top 10 queries against BigQuery in the past week, while listing calling users and referenced tables for each query.

sources:
  - Audit Logs - Data Access

use_cases:
  - Audit

test:

samples:

references: