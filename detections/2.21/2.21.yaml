---
id: '2.21'
name: permissions_granted_to_impersonate_SA
# Refer to corresponding queries: 2_21_permissions_granted_to_impersonate_SA.{sql|yaral|...}
display_name: Permissions granted to impersonate Service Account
category: IAM, Keys & Secrets Changes
severity: Medium
description: |
  Permissions granted to impersonate a service account

sources:
  - Audit Logs - Admin Activity

use_cases:
  - Detect
  - Audit

test:

samples:

references: