---
id: '3.10'
name: unusual_admin_activity_by_user_country
# Refer to corresponding queries: 3_10_unusual_admin_activity_by_user_country.{sql|yaral|...}
display_name: Unusual admin activity by user & country in the last 7 days
category: Cloud Provisioning Activity
severity: Medium
description: |
  Any new admin activity by a particular user from a given country first seen in the last 7 days.
  Default lookback window over all admin activity in the last 60 days.

sources:
  - Audit Logs - Admin Activity

use_cases:
  - Detect

test:

samples:

references: